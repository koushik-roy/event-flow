spring:
  application:
    name: eventflow
  datasource:
    url: jdbc:postgresql://localhost:5432/eventflow
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

